package game

// Version is rewritten by tagpr to match the git tag (see .tagpr), so it
// names the last release, not the working tree. It is here for the
// firmware to report; nothing reads it yet.
const Version = "0.0.0"
